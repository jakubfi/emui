set(PACKAGE_VERSION @EMUI_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL @EMUI_VERSION_MAJOR@)
    set(PACKAGE_VERSION_COMPATIBLE 1)
    if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL @EMUI_VERSION_MINOR@)
        set(PACKAGE_VERSION_EXACT 1)
    endif()
endif() 
